# 面向单图训练的图像筛选策略

## 1. 背景

当前数据的主要问题不是病人标签冲突，而是：

- 同一病人目录下的多张超声图虽然共享同一个病人级二分类标签
- 但这些图像之间的视觉信息差异很大
- 其中一部分图像可能是真正承载分类证据的主视图
- 另一部分图像则可能是弱信息图、冗余图、离群图，甚至是几乎无效的图

如果继续直接做病人级训练，那么：

- `mean pooling` 容易被大量弱信息图稀释
- `max pooling` 容易被离群图误触发
- 随机采样病人内图像也会带来较大不稳定性

因此，当前更推荐的方向是：

**先从原始病人目录中筛出更可能承载单图分类证据的图像，再直接做图像级训练与推理。**

---

## 2. 总体目标

筛图的目标不是证明“某张图一定等价于病人最终标签”，而是尽可能保守地筛出：

- 更高质量的图像
- 更可能承载病灶分类证据的图像
- 更少冗余、更少离群的图像

最终输出一个新的图像级 label JSON，使后续训练可以直接基于单图进行：

- 图像级训练
- 图像级验证
- 图像级推理

而不再把病人 bag 聚合作为主路线。

---

## 3. 适用范围

### 3.1 FTCPTC

该任务更适合先做筛图，再转成单图训练。

原因：

- 病灶本身通常在图像中可见
- 虽然不是每张图都同样有信息，但通常仍能筛出相对更像“主病灶视图”的图像
- 因此通过筛图降低噪声后，做单图训练是合理的

### 3.2 LNM_CN01

该任务也可以做筛图，但应更谨慎。

原因：

- 淋巴结转移标签本质上更像病人级或病例级结局
- 单张甲状腺超声图未必稳定携带足够强的直接证据
- 因此筛图可以降低噪声，但不能保证单图监督一定完全可靠

结论：

- `FTCPTC`：更推荐筛图后直接走单图训练
- `LNM_CN01`：可以尝试筛图后单图训练，但应保守评估效果，并与原方案做对比实验

---

## 4. 筛图的总体原则

### 4.1 先删明显坏图，再做优选

先处理低争议问题：

- 空 mask
- 极小 mask
- 严重碎片化 mask
- 明显离群的异常图
- 高冗余重复图

然后再在剩余图像中选择更适合训练的主视图。

### 4.2 不把“少见图”直接当错图

某张图在病人目录内很特别，不代表它一定错误。

它可能是：

- 真正最有信息的一张图
- 不同切面但仍然有用
- 成像条件变化下的主病灶图

因此：

- 离群图默认先降权
- 不建议一开始就因为“和其他图不一样”而自动删除

### 4.3 筛图目标是降低噪声，而不是最大化保留数量

筛图后样本数通常会下降，这是正常的。

更重要的是：

- 保留下来的图是否更稳定
- 单图监督是否更干净
- 类间可分性是否更强

---

## 5. 推荐的三层筛图策略

## 第一层：硬性删除规则

这一层只删低争议坏样本。

### 5.1 结构异常样本

直接删除：

- `filename` 缺失
- image 文件不存在
- mask 文件不存在
- 图像或 mask 无法读取

### 5.2 空 mask

直接删除：

- `foreground_pixels == 0`

因为这类图像无法提供可信病灶区域。

### 5.3 极小病灶图

直接删除或强烈怀疑：

- `mask_area_ratio` 过小
- `largest_component_pixels` 过小

这类图像常见问题：

- 病灶几乎不可见
- mask 只是一点噪声
- 单图分类信息极弱

### 5.4 严重碎片化 mask

建议删除或至少大幅降权：

- 最大连通域比例过低
- 小碎片过多
- 病灶区域明显不连续

这类图像通常对应：

- 标注不稳定
- 非主病灶图
- 信息不完整

---

## 第二层：病人内离群与弱信息图降权

这一层的目标不是立刻删图，而是给图像排序。

### 6.1 离群图识别

可使用病人内相对特征做 outlier 分析，重点关注：

- `mask_area_ratio`
- `bbox_fill_ratio`
- `largest_component_ratio`
- `center_x_ratio`
- `center_y_ratio`
- `edge_touch_penalty`
- `masked_intensity_mean`

如果某张图在同病人内部相对其他图偏离很大，则：

- 默认先降权
- 如果同时质量又差，可进一步删除

### 6.2 边缘病灶图降权

建议降权：

- 病灶 bbox 严重贴边
- 病灶只出现在画面角落
- 病灶主体可能被裁切

### 6.3 弱占据图降权

建议降权：

- 病灶在整张图中占比太小
- bbox 很大但病灶填充率低
- 说明主要是背景，病灶本体不突出

### 6.4 灰度/纹理明显异常图降权

建议降权：

- 病灶区域灰度分布与同病人其他图差异极大
- 图像亮度、对比度、纹理强度明显偏离病人主群

这类图像不一定错误，但往往不够稳定。

---

## 第三层：病人内去冗余与主视图保留

在通过前两层后，通常每个病人内部仍会保留多张图。

这时不应该全部等权使用，而应进一步优选。

### 7.1 去除高冗余图

如果同病人多张图高度相似：

- 只保留代表图
- 不需要把连续近重复图都留下

高冗余图的问题：

- 不会显著增加信息量
- 反而会放大某一视角的权重

### 7.2 保留 top-k 主视图

建议对每个病人只保留 top-k 张最适合单图训练的图。

排序依据可综合：

- mask 面积比例适中
- 最大连通域完整
- bbox 填充率高
- 病灶更居中
- 边缘惩罚更低
- 病人内离群程度更低
- 灰度/纹理特征更接近病人主群

### 7.3 建议的保留数量

#### FTCPTC

建议每病人保留：

- top 4
- top 6
- 或 top 8

因为该任务可适当保留一定视角多样性。

#### LNM_CN01

建议每病人保留：

- top 2
- top 4
- 或 top 6

因为该任务更应强调“更稳定、更干净”的少量主视图。

---

## 6. 适合单图训练的图像应具备哪些特征

更适合保留的图像通常满足：

- 病灶区域明确存在
- mask 不为空且不太小
- 最大连通域占主体
- bbox 填充率较高
- 病灶不过度贴边
- 在病人内部不是极端离群图
- 与其他保留图相比冗余不至于过高

不太适合单图训练的图像通常包括：

- 空 mask 图
- 病灶极小图
- 碎片化 mask 图
- 病灶严重贴边图
- 病人内强离群且质量差的图
- 多张几乎重复的冗余图

---

## 7. 对 FTCPTC 与 LNM_CN01 的具体建议

### 7.1 FTCPTC

推荐策略：

- 硬过滤用保守阈值
- 离群图先降权，不急于删除
- 允许保留更多视角多样性
- 每病人保留略多的 top-k

更适合的目标：

- 从病人目录中筛出更像“主病灶图”的图像
- 再做图像级训练

### 7.2 LNM_CN01

推荐策略：

- 硬过滤稍严格
- 对贴边、碎片化、弱占据图惩罚更强
- 对离群图更谨慎
- 每病人保留更少但更稳定的图像

更适合的目标：

- 尽量保留最干净、最稳定、最像主视图的少量图像
- 再做图像级训练，并与原始训练方式做对照

---

## 8. 推荐输出物

如果将该策略落地成脚本，建议输出：

1. `filtered_labels.json`
   - 可直接用于图像级训练

2. `summary.<target_key>.json`
   - 筛前/筛后图像数
   - 筛前/筛后病人数
   - 类别分布变化
   - 各类 drop reason 计数

3. `image_decisions.<target_key>.json`
   - 每张图为什么被保留、降权或删除

4. `patients.<target_key>.json`
   - 每病人的异质性分数
   - 保留图数量
   - 高冗余/高异质性情况

5. `review.<target_key>.json`
   - 需要人工复核的病人

---

## 9. 训练使用建议

筛图完成后，优先走图像级训练：

- `train_multitask_binary_sampler.py`
- `train_multitask_binary_gamtl.py`

只需把训练脚本中的：

- `TRAIN_LABEL_PATH`

替换成新的 `filtered_labels.json` 即可。

建议：

- `FTCPTC` 先做图像级训练主线
- `LNM_CN01` 先做筛图后图像级训练试验，再和原始数据方案比较

---

## 10. 一句话结论

当前最合理的方向不是继续把同病人所有图像等权用于病人级二分类，而是：

**先删除明显坏图、识别病人内离群与冗余图，再为每个病人保留少量更像主病灶视图的高质量图像，最终直接做图像级训练与推理。**
